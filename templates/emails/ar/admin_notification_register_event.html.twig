/* AR: templates/emails/ar/admin_notification_register_event.html.twig */

<!DOCTYPE html>
<html dir="rtl" lang="ar">

<head>
    <meta charset="UTF-8">
    <title>تسجيل جديد للحفلة الشواء</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
        }

        .container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }

        .header {
            background-color: #4CAF50;
            color: white;
            padding: 10px;
            text-align: center;
        }

        .content {
            padding: 20px;
            background-color: #f9f9f9;
        }

        .info {
            margin-bottom: 20px;
        }

        .guests {
            padding-right: 20px;
        }

        .footer {
            text-align: center;
            font-size: 12px;
            color: #777;
            margin-top: 20px;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="header">
            <h1>تسجيل جديد للحفلة الشواء</h1>
        </div>

        <div class="content">
            <p>مرحبا،</p>

            <p>تم تسجيل مشارك جديد لحفلة الشواء يوم الجمعة 30 مايو 2025.</p>

            <div class="info">
                <h3>معلومات المشارك:</h3>
                <p>
                    اللقب: {{ registration.lastName }}<br> الاسم: {{ registration.firstName }}<br> البريد الإلكتروني: {{
                    registration.email }}<br> الهاتف: {{ registration.phone }}<br> تاريخ التسجيل: {{
                    registration.registeredAt|date('d/m/Y في H:i') }}
                </p>
            </div>

            <div class="info">
                <h3>الضيوف:</h3>
                {% if registration.guests|length > 0 %}
                <ul class="guests">
                    {% for guest in registration.guests %}
                    <li>{{ guest.firstName }} {{ guest.lastName }}</li>
                    {% endfor %}
                </ul>
                {% else %}
                <p>لا يوجد ضيوف إضافيين.</p>
                {% endif %}
            </div>

            <p>إجمالي عدد المشاركين: {{ registration.guests|length + 1 }}</p>
        </div>

        <div class="footer">
            <p>تم إرسال هذه الرسالة تلقائيًا - يرجى عدم الرد عليها.</p>
        </div>
    </div>
</body>

</html>